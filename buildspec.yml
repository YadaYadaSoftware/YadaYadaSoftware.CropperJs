version: 0.2
env:
  shell: bash
phases:
  install:
    commands:
      - VERSION="${MAJOR}.$(date +%j).${CODEBUILD_BUILD_NUMBER}"
  build:
    commands:
      - dotnet pack --verbosity quiet --configuration $CONFIGURATION
